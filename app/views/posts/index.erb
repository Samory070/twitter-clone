<h1>All Tweets</h1>

<div class="posts">
  <ul>
    <% @posts.each do |post| %>
      <li><%= "#{post.id} - #{post.user_id} - #{post.body}" %> - <a href="/posts/<%= post.id %>">Show</a></li>
    <% end %>
  </ul>
</div>

